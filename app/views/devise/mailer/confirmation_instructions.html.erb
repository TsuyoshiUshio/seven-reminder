<div class="page-header">
<p>Welcome <%= @email %>!</p>
</div>
<p>You can confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token), :class => 'btn btn-link' %></p>
